# Expected Outputs

Store example JSON outputs here for each agent, aligned with files under `../examples/`.

These can be used as fixtures for local testing and CI to ensure the pipeline behaves as expected.
