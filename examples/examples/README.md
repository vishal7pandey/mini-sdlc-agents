# Example Inputs

Place raw requirement examples here (e.g., `.txt` or `.md`).

These files are intended to be fed into the pipeline (manually or via a future CLI) to generate structured artifacts for each agent.
